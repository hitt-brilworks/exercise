import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  card: {
    width: 200,
    height: 300,
    backGroundColor: "white",
  },
  image: {
    height: 170,
    width: 170,
  },
});
